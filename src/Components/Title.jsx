const Title = (props) => {
    return ( 
        <h1 className="text-36 my-[20px] text-Secondary whitespace-nowrap overflow-ellipsis overflow-hidden">{props.text}</h1>
     );
}
 
export default Title;